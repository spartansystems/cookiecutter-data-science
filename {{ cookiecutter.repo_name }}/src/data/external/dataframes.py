def users:

def messages:
    pd.DataFrame.new

get_df(.filter(team='Spartan'))

dataframes.Messages.filter(team='Spartan')).get_df


class Message:
    model = models.Message
    __init__(self):
        self.query = db.session.query(self.model)

    filter(condition):
        self.query

    def get_df():

        self.query.all()
